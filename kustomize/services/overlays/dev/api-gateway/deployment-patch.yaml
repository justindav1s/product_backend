kind: Deployment
apiVersion: apps/v1
metadata:
  name: deployment
spec:
  template:
    spec:
      serviceAccountName: api-gateway-sa
      containers:
        - name: container 
          image: nexus3-docker-cicd.apps-crc.testing/repository/docker/amazin-dev/api-gateway:6e6ef00
          env:
          - name: SPRING_PROFILES_ACTIVE
            value: dev
          volumeMounts:
            - mountPath: /config
              name: api-gateway-config-vol      
      volumes:
      - configMap:
          defaultMode: 420
          name: api-gateway-config
        name: api-gateway-config-vol                                     