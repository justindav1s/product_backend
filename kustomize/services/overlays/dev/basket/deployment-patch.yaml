kind: Deployment
apiVersion: apps/v1
metadata:
  name: deployment
spec:
  template:
    spec:
      serviceAccountName: basket-sa
      containers:
        - name: container 
          image: nexus3-docker-cicd.apps-crc.testing/repository/docker/amazin-dev/basket:6e6ef00
          env:
          - name: SPRING_PROFILES_ACTIVE
            value: dev
          volumeMounts:
            - mountPath: /config
              name: basket-config-vol      
      volumes:
      - configMap:
          defaultMode: 420
          name: basket-config
        name: basket-config-vol                                     